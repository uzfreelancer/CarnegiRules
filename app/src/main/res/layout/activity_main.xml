<?xml version="1.0" encoding="utf-8"?>
<ScrollView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context="com.example.android.carnegirules.MainActivity">
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="vertical">
        <ImageView
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:scaleType="center"
            android:src="@drawable/carnegi"/>
    <Button
        android:id="@+id/rules1"
        android:layout_width="match_parent"
        android:layout_height="48dp"
        android:layout_marginTop="16dp"
        android:background="@color/colorA"
        android:textColor="@color/colorBtnText"
        android:text="Как расположить к себе людей"
        android:onClick="show"/>
    <Button
        android:id="@+id/rules2"
        android:layout_width="match_parent"
        android:layout_height="48dp"
        android:layout_marginTop="16dp"
        android:background="@color/colorB"
        android:textColor="@color/colorBtnText"
        android:text="Как убеждать в своей точке зрения"
        android:onClick="show"/>
    <Button
        android:id="@+id/rules3"
        android:layout_width="match_parent"
        android:layout_height="48dp"
        android:layout_marginTop="16dp"
        android:background="@color/colorC"
        android:textColor="@color/colorBtnText"
        android:text="Как изменить человека"
        android:onClick="show"/>
    <Button
        android:id="@+id/rules4"
        android:layout_width="match_parent"
        android:layout_height="48dp"
        android:layout_marginTop="16dp"
        android:background="@color/colorD"
        android:textColor="@color/colorBtnText"
        android:text="Счастливая семейная жизнь"
        android:onClick="show"/>
    </LinearLayout>
</ScrollView>
